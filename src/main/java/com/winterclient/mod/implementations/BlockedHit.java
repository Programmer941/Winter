package com.winterclient.mod.implementations;

import com.winterclient.mod.HUDMod;

public class BlockedHit extends HUDMod {
    public BlockedHit(int x, int y, int width, int height, boolean lockedScale) {
        super(x, y, width, height, lockedScale);
    }
}
